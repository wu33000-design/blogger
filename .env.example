# Canonical frontend origin
SITE_URL=https://blogger-internal.wu33000.workers.dev

# Supabase browser client. Use the Project URL and publishable key from Connect.
# Authorization is enforced by Supabase Auth + Postgres Row Level Security.
PUBLIC_SUPABASE_URL=
PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Build-time server credential. Never expose this as PUBLIC_* or to browser code.
SUPABASE_SERVICE_ROLE_KEY=

# Legacy Ghost Content API (temporary migration fallback only)
GHOST_URL=
GHOST_KEY=
