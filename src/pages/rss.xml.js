import { getPosts } from '../lib/posts.js';
import { getSiteContent } from '../lib/site-content.js';
const esc=s=>String(s??'').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'","&apos;");
export async function GET({site,url}){const posts=await getPosts();const content=await getSiteContent('zh');const siteTitle=content['site.title']||'BACK MOUNTAIN';const description=content['home.intro']||'Notes on technology, design, and research.';const base=site?.toString()||url.origin+'/';const items=posts.map(p=>`<item><title>${esc(p.title)}</title><link>${new URL('/post/'+p.slug,base)}</link><guid>${new URL('/post/'+p.slug,base)}</guid><pubDate>${new Date(p.published_at).toUTCString()}</pubDate><description>${esc(p.excerpt)}</description></item>`).join('');return new Response(`<?xml version="1.0" encoding="UTF-8"?><rss version="2.0"><channel><title>${esc(siteTitle)}</title><link>${base}</link><description>${esc(description)}</description>${items}</channel></rss>`,{headers:{'Content-Type':'application/rss+xml; charset=utf-8'}})}